# requirements.txt for SmileVision Streamlit App

streamlit==1.36.0       # Web app framework for the UI
pillow==10.4.0          # Image processing library (PIL fork)
numpy==1.26.4           # Numerical operations for image manipulation
opencv-python==4.10.0.84 # OpenCV for basic image processing (cv2)
